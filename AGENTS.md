# re.kriate Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-08

## Active Technologies
- Markdown + JSON schema contracts + bash workflow scripts + `git`, GitHub metadata, multiclaude branch context, `docs/code-review.html`, `docs/branch-gap-analysis.html`, Spec Kit templates (001-branch-review-followups)
- Versioned docs under `docs/` and spec artifacts under `specs/001-branch-review-followups/` (001-branch-review-followups)

- Markdown specifications + shell-driven workflow (bash scripts under `.specify/scripts/bash/`) + `git` branch/PR metadata, repository docs, existing diagram artifacts, Spec Kit templates (001-branch-review-followups)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Markdown specifications + shell-driven workflow (bash scripts under `.specify/scripts/bash/`)

## Code Style

Markdown specifications + shell-driven workflow (bash scripts under `.specify/scripts/bash/`): Follow standard conventions

## Recent Changes
- 001-branch-review-followups: Added Markdown + JSON schema contracts + bash workflow scripts + `git`, GitHub metadata, multiclaude branch context, `docs/code-review.html`, `docs/branch-gap-analysis.html`, Spec Kit templates

- 001-branch-review-followups: Added Markdown specifications + shell-driven workflow (bash scripts under `.specify/scripts/bash/`) + `git` branch/PR metadata, repository docs, existing diagram artifacts, Spec Kit templates

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
